export = MemoryBlockstore;
/**
 * @typedef {import('./types').Pair} Pair
 * @typedef {import('./types').Blockstore} Blockstore
 * @typedef {import('interface-store').Options} Options
 */
/**
 * @class MemoryBlockstore
 * @implements {Blockstore}
 */
declare class MemoryBlockstore extends Adapter implements Blockstore {
    /** @type {Record<string, Uint8Array>} */
    data: Record<string, Uint8Array>;
}
declare namespace MemoryBlockstore {
    export { Pair, Blockstore, Options };
}
type Blockstore = import('./types').Blockstore;
import Adapter = require("./adapter");
type Pair = import('./types').Pair;
type Options = import('interface-store').Options;
//# sourceMappingURL=memory.d.ts.map