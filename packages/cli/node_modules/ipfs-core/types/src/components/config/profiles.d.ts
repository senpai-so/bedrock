/**
 * @typedef {import('ipfs-core-types/src/config').Config} Config
 *
 * @typedef {object} Transformer
 * @property {string} description
 * @property {(config: Config) => Config} transform
 */
/**
 * @type {Record<string, Transformer>}
 */
export const profiles: Record<string, Transformer>;
export type Config = import('ipfs-core-types/src/config').Config;
export type Transformer = {
    description: string;
    transform: (config: Config) => Config;
};
//# sourceMappingURL=profiles.d.ts.map