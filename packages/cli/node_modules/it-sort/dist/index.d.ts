export = sort;
/**
 * Collects all values from an async iterator, sorts them
 * using the passed function and yields them
 *
 * @template T
 * @param {AsyncIterable<T> | Iterable<T>} source
 * @param {(a: T, b: T) => -1 | 0 | 1} sorter
 */
declare function sort<T>(source: AsyncIterable<T> | Iterable<T>, sorter: (a: T, b: T) => -1 | 0 | 1): AsyncGenerator<T, void, undefined>;
//# sourceMappingURL=index.d.ts.map