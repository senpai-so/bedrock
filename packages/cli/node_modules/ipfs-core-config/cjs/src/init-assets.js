'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var all = require('it-all');
var index = require('./init-files/init-docs/index.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var all__default = /*#__PURE__*/_interopDefaultLegacy(all);

async function initAssets({addAll, print}) {
  const results = await all__default["default"](addAll(index, { preload: false }));
  const dir = results.filter(file => file.path === 'init-docs').pop();
  if (!dir) {
    return;
  }
  print('to get started, enter:\n');
  print(`\tjsipfs cat /ipfs/${ dir.cid }/readme\n`);
}

exports.initAssets = initAssets;
