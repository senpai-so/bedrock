/**
 * Select the best record out of the given records.
 *
 * @param {import('libp2p-interfaces/src/types').DhtSelectors} selectors
 * @param {Uint8Array} k
 * @param {Array<Uint8Array>} records
 */
export function bestRecord(selectors: import('libp2p-interfaces/src/types').DhtSelectors, k: Uint8Array, records: Array<Uint8Array>): number;
export declare const selectors: {
    pk: (k: Uint8Array, records: Uint8Array[]) => number;
};
//# sourceMappingURL=selection.d.ts.map